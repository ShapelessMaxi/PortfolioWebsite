class interest {
    constructor(
      title,
      summary,
      description,
      tag,
    ) {
      this.title = title;
      this.summary = summary;
      this.description = description;
      this.tag = tag;
    }
  }